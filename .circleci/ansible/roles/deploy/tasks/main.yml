--- 
- name: "Copy file with owner and permissions"
  copy: 
    dest: /home/ubuntu/
    src: ./

- name: "Start Server"
  become: true
  raw: cd ~/cdond-c3-projectstarter/backend/; apt install node-typescript -y; npm install; npm install ts-node; pm2 start npm -- run start